class CommandExecutor:
    def __init__(self, *args, **kwargs):
        raise RuntimeError("daemon commands are not included in AgentSecure Community")


class CommandPoller:
    def __init__(self, *args, **kwargs):
        raise RuntimeError("daemon polling is not included in AgentSecure Community")
